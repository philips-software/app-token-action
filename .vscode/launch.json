{
    "configurations": [
      {
        "type": "node-terminal",
        "name": "Debug Current TS File (ts-node)",
        "request": "launch",
        "command": "yarn run ts-node -- ${fileDirname}/${fileBasenameNoExtension}",
        "cwd": "${workspaceFolder}"
      },
      {
        "type": "node",
        "name": "Debug ",
        "request": "launch",
        "runtimeExecutable": "yarn",
        "runtimeArgs": ["ts-node ${fileDirname}/${fileBasename}"],
        "cwd": "${workspaceFolder}"

      }
    ]
  }
  